<source>
  @type forward
  port 24224  # Fluentd가 수집하는 포트
  bind 0.0.0.0  # 모든 네트워크 인터페이스에서 수신
</source>


<match **>
  @type forward
  <server>
    host 172.31.25.202
    port 24224
  </server>
</match>
